; Ensure PHP-FPM runs in foreground so Docker can manage the process lifecycle.
[global]
daemonize = no

; Override listen address so nginx (on a different container) can reach it.
; The base docker.conf already sets listen = 9000 (all interfaces) which is fine.
[www]
listen = 0.0.0.0:9000
